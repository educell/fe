const initialState = {
    students: [],
    schools: [],
    isLoggedIn: false,
    isLoggingIn: false,
    error: null,
    
};

const reducer = (state = initialState, action) => {
    switch(action.type){

    }
};

export default reducer;